{
  "buildCommand": "cd apps/student && npm run build",
  "outputDirectory": "apps/student/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
